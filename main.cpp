#include <iostream>
#include <vector> // Have a vector of some pre-programmed rocket parts so the user can just choose the parts instead of creating one
#include <fstream> // Used to save something to a file

int main()
{

	return 0;
}